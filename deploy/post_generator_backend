server {
    listen 80;
    server_name 178.154.194.84;
    location = /favicon.ico { access_log off; log_not_found off; }
    location /static/ {
        root /home/romantihiy/post_generator_backend/backend;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/run/post_generator_backend.sock;
    }
}
